#test_that("compute_risk works as expected.", {
#  val <- compute_risk(0.2, pseudo_pop_covar_test$Y,
#                      pseudo_pop_covar_test$w, seq(0.2,10,0.2))

#  expect_equal(val, 40.44144, tolerance=0.0001)
#})
